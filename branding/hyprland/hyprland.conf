$mod = SUPER
$terminal = kitty
$fileManager = thunar
$menu = wofi --show drun

monitor = , preferred, auto, 1

exec-once = swaybg -i /usr/share/wallpapers/Najs/contents/images/2560x1600.png -m fill
exec-once = waybar
exec-once = nm-applet --indicator
exec-once = mako
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

input {
    follow_mouse = 1

    touchpad {
        natural_scroll = true
    }
}

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(55d7ffff) rgba(278abaff) 45deg
    col.inactive_border = rgba(284457aa)
    layout = dwindle
}

decoration {
    rounding = 10

    blur {
        enabled = true
        size = 6
        passes = 2
    }

    shadow {
        enabled = true
        range = 8
        render_power = 3
        color = rgba(00000066)
    }
}

animations {
    enabled = true
    bezier = najs, 0.2, 0.8, 0.2, 1.0
    animation = windows, 1, 4, najs
    animation = fade, 1, 4, najs
    animation = workspaces, 1, 4, najs
}

misc {
    disable_hyprland_logo = true
    force_default_wallpaper = 0
}

bind = $mod, Return, exec, $terminal
bind = $mod, E, exec, $fileManager
bind = $mod, R, exec, $menu
bind = $mod, Q, killactive
bind = $mod, F, fullscreen
bind = $mod, V, togglefloating
bind = $mod, L, exec, swaylock -f -c 07111c
bind = $mod SHIFT, M, exit
bind = $mod, Print, exec, grim -g "$(slurp)" - | wl-copy

bind = $mod, left, movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up, movefocus, u
bind = $mod, down, movefocus, d

bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5

bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = , XF86MonBrightnessUp, exec, brightnessctl set +5%
bindel = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
