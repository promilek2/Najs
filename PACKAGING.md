# Packaging

Arch packages are the upstream source for v1. Najs-specific packages will use
PKGBUILDs, reproducible source archives, detached package signatures, and a
small signed repository. The repository does not yet exist, and the live image
temporarily installs the locally built CLI directly into its root filesystem.
